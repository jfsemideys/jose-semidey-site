import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

const ResumeHeader = styled(Box)(() => ({
    display: 'flex', 
    justifyContext: 'center', 
    alignItems:'center', 
    flexDirection: 'column',
  }));

  export default ResumeHeader;